package hw4.maze;

// Represents possible movement directions in the grid
public enum Direction {
    // Move upward in the grid
    UP,
    // Move downward in the grid
    DOWN,
    // Move leftward in the grid
    LEFT,
    // Move rightward in the grid
    RIGHT
} 